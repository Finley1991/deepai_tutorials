基本要求：
1、代码添加注释（思考）
2、抄写代码至少两遍（思考）

作业要求：
1、改进样本生成方式，每一张图像生成的每一类样本随机添加（模糊，噪声，暗度，两两组合，三者组合）
2、改进训练方式（要求使用自动停止训练）
3、改进采样方法，使用transform的经验值
#torchvision.transforms.Normalize(mean=[0.485, 0.456, 0.406], std=[0.229, 0.224, 0.225])

4、改进预测网络的计算方式（循环改矩阵）
5、改成摄像头预测
6、对摄像头输出图像做预处理，和原始输出作对比。